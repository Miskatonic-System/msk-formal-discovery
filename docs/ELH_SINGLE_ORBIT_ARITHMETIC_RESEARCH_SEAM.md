# ELH Single-Orbit Arithmetic Research Seam

**Status:** `PARKED_RESEARCH_SEAM`  
**Authority:** `NONE`  
**Upstream digestion:** `Miskatonic-System/miskatonic-mathematics/docs/digestions/ELH_V1_0_DIGESTION.md`  
**Bound source SHA-256:** `524e870fc28deed53541784688b3d8f2677628c303eb83689fcc93a402aea152`

## Purpose

Record the future `msk-formal-discovery` role for the Ergodic Lattice Hypothesis (ELH) research direction after Mathematical Metabolism digestion.

Formal Discovery does not own the normality claim and does not treat transfer-operator ensemble mixing as proof of a specific arithmetic orbit.

The executable/scientific seam is the exact single-orbit problem:

```text
BBP identity
  -> exact perturbation sequence
  -> exact Bailey-Crandall recurrence
  -> certified orbit representation
  -> Weyl / discrepancy observations
  -> candidate arithmetic structure
  -> non-authoritative lemmas / proof candidates
```

## Core firewall

```text
ENSEMBLE_DENSITY_MIXING
!=
SINGLE_ORBIT_EQUIDISTRIBUTION
```

The non-autonomous transfer-operator lane is permitted as a control/baseline provider but must not satisfy the arithmetic single-orbit obligation by substitution.

## Pi-specific asymptotic seam

The source perturbation for the pi BBP combination has cancellation of the `1/n` term and should be treated separately from generic BBP families.

```text
PI_SPECIFIC_PERTURBATION = O(n^-2)_CANDIDATE
GENERIC_BBP_PERTURBATION = MAY_BE_O(n^-1)
GENERIC_BBP_ASYMPTOTIC != PI_SPECIFIC_ASYMPTOTIC
```

Any future implementation must derive and test the exact coefficients rather than inheriting the charter's generic logarithmic-drift language.

## Candidate provider roles

Formal Discovery may eventually provide:

- exact symbolic expansion of the perturbation sequence;
- independent recurrence implementations;
- direct BBP modular digit/orbit implementations;
- arbitrary-precision or interval-certified reference execution;
- Weyl-sum and discrepancy measurement harnesses;
- arithmetic resonance / near-return detectors;
- denominator-structure feature extraction;
- candidate van-der-Corput or differencing transformations;
- candidate 2-adic / 16-adic structure hypotheses;
- held-out comparison of candidate lemmas or transformations;
- non-authoritative proof/refactoring candidates.

None of these automatically confer theorem authority.

## Execution custody

Before numerical statistics are interpreted, future experiments should require cross-representation orbit parity:

```text
RECURRENCE_IMPLEMENTATION
DIRECT_BBP_MODULAR_IMPLEMENTATION
HIGH_PRECISION_OR_CERTIFIED_REFERENCE
```

with preregistered agreement windows.

Permanent firewall:

```text
FLOAT64_PSEUDO_ORBIT != EXACT_BAILEY_CRANDALL_ORBIT
```

For any new empirical campaign, the existing Miskatonic Empirical Assurance standard applies, including exact source identity, execution-source sealing, declared and observed budgets, runtime provenance, fail-closed partial execution, and immutable result receipts.

## Falsification semantics

Candidate strong rates may be tested, but stronger-rate failure must not be promoted into non-normality.

```text
NO_SQRT_CANCELLATION != NOT_NORMAL
WEAKER_DISCREPANCY_RATE != NON_UNIFORM_DISTRIBUTION
FINITE_NUMERICAL_UNIFORMITY != ASYMPTOTIC_NORMALITY
```

## Formalization handoff

Formal Discovery may provide exact identities and bounded computational evidence to later formalization/proof-custody layers.

Recommended earned sequence:

```text
1. exact perturbation definition
2. recurrence well-defined mod 1
3. finite unrolling identity
4. source alignment to BBP fractional parts
5. Weyl criterion specialization
6. only then candidate normality bridge obligations
```

A theorem skeleton concluding `True`, using `sorry`, or otherwise failing statement alignment remains `FORMALIZATION_SKELETON` and carries no theorem authority.

## Application quarantine

Do not route ELH search results directly into cryptographic or quantum claims.

```text
NORMALITY != CRYPTOGRAPHIC_SECURITY
EQUIDISTRIBUTION != NEXT_BIT_UNPREDICTABILITY
CLASSICAL_COCYCLE_CONTRACTION != QUANTUM_THERMALIZATION
```

Such bridges require independent domain programs and evidence.

## Suggested future experiment sequence

This record authorizes no execution. If activated later:

```text
ELH-FD-00 exact symbolic/asymptotic parity
ELH-FD-01 certified finite-orbit engine
ELH-FD-02 multi-harmonic Weyl/discrepancy baseline
ELH-FD-03 arithmetic-resonance feature study
ELH-FD-04 candidate single-orbit transformations/lemmas
ELH-FD-05 held-out utility and independent reconstruction
```

The transfer-operator control should remain a separately labeled arm, e.g.:

```text
ELH-FD-CONTROL-TO
```

## Claim ceiling

```text
BASE_16_NORMALITY = NOT_ESTABLISHED
SINGLE_ORBIT_EQUIDISTRIBUTION = NOT_ESTABLISHED
ENSEMBLE_TRANSFER_OPERATOR_BEHAVIOR = CONTROL_ONLY_UNTIL_QUALIFIED
EXECUTION_AUTHORITY = NONE
SCIENTIFIC_AUTHORITY = NONE
CANONICAL_LIBRARY_MUTATION = FALSE
```
